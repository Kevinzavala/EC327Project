package com.example.main_screen;

import android.content.Intent;
import android.support.annotation.NonNull;
import android.support.design.widget.BottomNavigationView;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.MenuItem;
import android.widget.ArrayAdapter;
import android.widget.ListView;

import java.util.ArrayList;
import java.util.List;
import java.util.Random;

public class back extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_back);

        ListView listView;

        String[] cardio;
        String[] back;

        Random random = new Random();
        int one, two, three, four, five, six;

        //random numbers from 0-10 and 0-15
        one = random.nextInt(10);
        two = random.nextInt(10);
        three = random.nextInt(15);
        four = random.nextInt(15);
        five = random.nextInt(15);
        six = random.nextInt(15);

        BottomNavigationView navigation = findViewById(R.id.navigation);

        listView = (ListView) findViewById(R.id.listview);
        cardio = getResources().getStringArray(R.array.workout_cardio);
        back = getResources().getStringArray(R.array.workout_back);

        //creates new list based on random workouts from cardio and back
        List<String> list = new ArrayList<>();

        list.add(cardio[one]);
        list.add(cardio[two]);
        list.add(back[three]);
        list.add(back[four]);
        list.add(back[five]);
        list.add(back[six]);

        ArrayAdapter<String> arrayAdapter = new ArrayAdapter<String>(this, android.R.layout.simple_list_item_1, list);
        listView.setAdapter(arrayAdapter);

        //sets the navigation bar
        navigation.setOnNavigationItemSelectedListener(new BottomNavigationView.OnNavigationItemSelectedListener() {
            @Override
            public boolean onNavigationItemSelected(@NonNull MenuItem item) {
                switch (item.getItemId()) {
                    case R.id.navigation_home:
                        Intent int1 = new Intent(back.this, main_menu.class);
                        startActivity(int1);
                        break;
                    case R.id.navigation_profile:
                        Intent int2 = new Intent(back.this, profile.class);
                        startActivity(int2);
                        break;
                    case R.id.navigation_workouts:
                        Intent int3 = new Intent(back.this, workouts.class);
                        startActivity(int3);
                        break;
                }
                return false;
            }
        });
    }
}
